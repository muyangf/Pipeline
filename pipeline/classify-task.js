module.exports = async ({ input }) => {
  return `
# 任务分类（Classify Task）

任务描述：
${input}

---

信息不足检查：
- 如果任务描述不清晰，请先提问。

禁止事项：
- 不得随意猜测任务类型
- 不得改变输出结构

---

# 任务说明（中文）

请将任务分类到对应层与 handler。

---

# 输出格式（必须严格遵守）

## 任务类型判断
- 类型：需求澄清 / 方案设计 / 任务拆分 / 实现 / 调试 / 审查 / 重构 / 归档 / 流程编排
- 理由：……

## 推荐入口
- 命令：\`/...\`
- 理由：……

## 备选入口
1. 命令：\`/...\`
   适用场景：……
2. 命令：\`/...\`
   适用场景：……

所有内容必须使用中文。
`;
};
