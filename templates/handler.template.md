# 通用 Handler 模板（Pipeline OS 3.0）

module.exports = async ({ input }) => {
  return `
# 标题（根据具体 handler 替换）

输入内容：
${input}

---

信息不足检查：
- 如果信息不足，请先用中文列出你需要的补充信息，而不是直接生成结果。

禁止事项：
- 不得自动执行任何命令
- 不得自动写入任何文件
- 不得假设外部系统状态
- 不得改变输出结构
- 不得新增或删除字段标题

---

# 任务说明（中文）

（这里用中文说明该 handler 的职责、目标、约束）

---

# 输出格式（必须严格遵守）

## 字段一
- ……

## 字段二
- ……

## 下一步建议
- 命令：\`/...\`
- 理由：……

所有内容必须使用中文。
`;
};