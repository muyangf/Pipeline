/*
 * 智能变更日志生成器（Changelog Generator 2.0）
 *
 * 目标：
 * - 将归档记录转换为最终的 changelog 条目
 * - 输出结构化、中文、可直接写入 CHANGELOG.md 的内容
 * - 与 OpenSpec 的“变更记录规范”保持一致
 *
 * 特点：
 * - 不执行实际文件写入（保持半自动）
 * - 完全离线可用
 */

module.exports = async ({ input }) => {
  return `
# 变更日志（Changelog Entry）

归档记录：
${input}

---

请根据以上归档记录生成一条“变更日志（Changelog Entry）”，并严格遵守以下要求：

1. **必须结构化、中文、简洁**
2. **必须包含以下字段：**
   - 变更类型（新增 / 修改 / 删除 / 修复 / 重构）
   - 变更内容（简洁中文）
   - 影响范围（简述）
   - 风险等级（低 / 中 / 高）
   - 日期（自动生成：使用“YYYY-MM-DD”格式）
3. **必须可直接写入 CHANGELOG.md**
4. **不得包含过长说明（每条不超过 3 行）**
5. **如果信息不足，请先提问**

---

# 输出格式（必须严格遵守）

## Changelog Entry
- 日期：YYYY-MM-DD
- 类型：……
- 内容：……
- 影响范围：……
- 风险等级：……

所有内容必须使用中文。
`;
};
