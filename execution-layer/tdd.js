/*
 * 智能 TDD 引导壳（TDD Wrapper 2.0）
 *
 * 目标：
 * - 为 Superpowers 的 TDD 能力提供结构化、中文化、离线友好的上层 Prompt
 * - 自动提醒模型遵守智能 code-style-smart（业务代码 / AI 编排代码自动识别）
 * - 强制 RED → GREEN → REFACTOR 流程
 * - 强制中文注释、中文说明、中文测试描述
 * - 输出“下一步建议”，与 next-step 2.0 完美衔接
 *
 * 特点：
 * - 不直接实现 TDD（底层由 Superpowers 完成）
 * - 这是“包装器（Wrapper）”，负责结构、规范、中文化、稳定性
 * - 完全离线可用
 */

module.exports = async ({ input }) => {
  return `
# 测试驱动开发（TDD）

任务描述：
${input}

---

请严格按照 **RED → GREEN → REFACTOR** 的流程进行设计与实现，输出内容必须包含：

1. **测试用例设计（中文说明）**
   - 用中文解释每个测试的目的、输入、预期输出
   - 不得省略

2. **测试代码（优先使用项目默认语言）**
   - 必须先写失败测试（RED）
   - 测试代码必须包含中文注释

3. **实现代码（GREEN）**
   - 仅实现让测试通过的最小逻辑
   - 所有注释必须中文

4. **重构建议（REFACTOR）**
   - 如有必要，给出结构优化建议
   - 必须中文说明理由

5. **下一步建议**
   - 例如：是否需要 code-review、是否需要 refactor、是否需要 archive
   - 用中文说明理由

---

# 代码生成规范（必须遵守）

你必须遵循以下规范：

## 1. 智能代码规范（自动识别）
- 自动识别当前任务更像：
  - **业务代码**（Controller / Service / Repository）
  - **AI 编排代码**（LangGraph / Agent / Tool / Node / State）
- 根据识别结果自动遵守：
  - /presets/business-code-style.md
  - /presets/ai-code-style.md

## 2. 中文优先
- 所有注释必须中文
- 所有测试描述必须中文
- 所有说明必须中文

## 3. 离线模型友好
- 输出结构必须稳定
- 不得生成过长代码（超过 200 行）
- 不得生成复杂 Markdown（如嵌套表格）

## 4. 信息不足时必须先提问
如果任务描述不足以开始 TDD，请先用中文说明你需要哪些补充信息，而不是自行假设。

---

# 输出格式（必须严格遵守）

请严格按照以下结构输出：

## 测试用例设计（中文说明）
- ……

## 测试代码（RED）
\`\`\`
（测试代码）
\`\`\`

## 实现代码（GREEN）
\`\`\`
（实现代码）
\`\`\`

## 重构建议（REFACTOR）
- ……

## 下一步建议
- ……

所有内容必须使用中文。
`;
};
