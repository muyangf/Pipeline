/*
 * 智能子代理引导壳（Subagent Wrapper 2.0）
 *
 * 目标：
 * - 为 Superpowers 的 subagent 能力提供结构化、中文化、离线友好的上层 Prompt
 * - 自动识别任务是否属于 AI 编排（Graph / Node / Tool / Agent）
 * - 自动要求生成：子代理职责、输入输出、节点结构、工具调用点、状态流转
 * - 强制中文注释、中文说明、中文结构
 * - 输出“下一步建议”，与 next-step 2.0 完美衔接
 *
 * 特点：
 * - 不直接实现子代理逻辑（底层由 Superpowers 完成）
 * - 这是“包装器（Wrapper）”，负责结构、规范、中文化、稳定性
 * - 完全离线可用
 */

module.exports = async ({ input }) => {
  return `
# 子代理开发（Subagent）

任务描述：
${input}

---

请根据任务内容设计并实现一个“子代理（Subagent）”，并严格遵守以下要求：

1. **子代理职责说明（中文）**
   - 用中文解释该子代理的职责、目标、边界
   - 必须包含：输入、输出、核心逻辑、依赖关系

2. **子代理结构设计（中文）**
   - 如果是 AI 编排任务，请生成：
     - 节点列表（Nodes）
     - 工具调用点（Tools）
     - 状态流转（State Flow）
     - 输入输出结构（IO Schema）
   - 所有说明必须中文

3. **子代理代码（必须遵守智能代码规范）**
   - 自动识别当前任务更像：
     - AI 编排代码（LangGraph / Agent / Tool / Node / State）
     - 或业务代码（如混合，请说明）
   - 根据识别结果自动遵守：
     - /presets/ai-code-style.md
     - /presets/business-code-style.md
   - 所有注释必须中文
   - 所有节点、工具、状态必须有中文说明

4. **测试代码（如适用）**
   - 如果子代理包含逻辑分支、状态变化、工具调用，请生成测试代码
   - 测试必须包含中文说明

5. **边界情况与错误处理（中文）**
   - 列出可能的异常情况
   - 给出处理策略

6. **下一步建议**
   - 例如：是否需要 code-review、是否需要 refactor、是否需要 archive
   - 用中文说明理由

---

# 代码生成规范（必须遵守）

## 1. 智能代码规范（自动识别）
- 自动识别当前任务更像：
  - AI 编排代码（LangGraph / Agent / Tool / Node / State）
  - 业务代码（Controller / Service / Repository）
- 根据识别结果自动遵守：
  - /presets/ai-code-style.md
  - /presets/business-code-style.md

## 2. 中文优先
- 所有注释必须中文
- 所有说明必须中文
- 所有测试描述必须中文

## 3. 离线模型友好
- 输出结构必须稳定
- 不得生成过长代码（超过 200 行）
- 不得生成复杂 Markdown（如嵌套表格）

## 4. 信息不足时必须先提问
如果任务描述不足以开始子代理设计，请先用中文说明你需要哪些补充信息，而不是自行假设。

---

# 输出格式（必须严格遵守）

请严格按照以下结构输出：

## 子代理职责说明（中文）
- ……

## 子代理结构设计（中文）
- 节点列表：
- 工具调用点：
- 状态流转：
- 输入输出结构：

## 子代理代码
\`\`\`
（子代理代码）
\`\`\`

## 测试代码（如适用）
\`\`\`
（测试代码）
\`\`\`

## 边界情况与错误处理
- ……

## 下一步建议
- ……

所有内容必须使用中文。
`;
};
