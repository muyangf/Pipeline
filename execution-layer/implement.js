/*
 * 智能实现引导壳（Implement Wrapper 2.0）
 *
 * 目标：
 * - 为 Superpowers 的 implement 能力提供结构化、中文化、离线友好的上层 Prompt
 * - 自动提醒模型遵守智能 code-style-smart（业务代码 / AI 编排代码自动识别）
 * - 强制中文注释、中文说明、中文结构
 * - 输出“下一步建议”，与 next-step 2.0 完美衔接
 *
 * 特点：
 * - 不直接实现逻辑（底层由 Superpowers 完成）
 * - 这是“包装器（Wrapper）”，负责结构、规范、中文化、稳定性
 * - 完全离线可用
 */

module.exports = async ({ input }) => {
  return `
# 功能实现（Implement）

任务描述：
${input}

---

请根据任务内容直接进行功能实现，但必须遵守以下要求：

1. **实现说明（中文）**
   - 用中文解释你将如何实现该功能
   - 必须包含：输入、输出、核心逻辑、边界情况

2. **实现代码（必须遵守智能代码规范）**
   - 自动识别当前任务更像：
     - 业务代码（Controller / Service / Repository）
     - AI 编排代码（LangGraph / Agent / Tool / Node / State）
   - 根据识别结果自动遵守：
     - /presets/business-code-style.md
     - /presets/ai-code-style.md
   - 所有注释必须中文
   - 所有函数、类、节点必须有中文说明

3. **测试代码（如适用）**
   - 如果任务涉及逻辑分支、数据处理、状态变化，请生成测试代码
   - 测试必须包含中文说明

4. **边界情况与错误处理（中文）**
   - 列出可能的异常情况
   - 给出处理策略

5. **下一步建议**
   - 例如：是否需要 code-review、是否需要 refactor、是否需要 archive
   - 用中文说明理由

---

# 代码生成规范（必须遵守）

## 1. 智能代码规范（自动识别）
- 自动识别当前任务更像：
  - 业务代码（Controller / Service / Repository）
  - AI 编排代码（LangGraph / Agent / Tool / Node / State）
- 根据识别结果自动遵守：
  - /presets/business-code-style.md
  - /presets/ai-code-style.md

## 2. 中文优先
- 所有注释必须中文
- 所有说明必须中文
- 所有测试描述必须中文

## 3. 离线模型友好
- 输出结构必须稳定
- 不得生成过长代码（超过 200 行）
- 不得生成复杂 Markdown（如嵌套表格）

## 4. 信息不足时必须先提问
如果任务描述不足以开始实现，请先用中文说明你需要哪些补充信息，而不是自行假设。

---

# 输出格式（必须严格遵守）

请严格按照以下结构输出：

## 实现说明（中文）
- ……

## 实现代码
\`\`\`
（实现代码）
\`\`\`

## 测试代码（如适用）
\`\`\`
（测试代码）
\`\`\`

## 边界情况与错误处理
- ……

## 下一步建议
- ……

所有内容必须使用中文。
`;
};
